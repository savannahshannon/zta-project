server {
    listen 80;
    
    location / {
        # Check for the "Company Laptop" Header
        if ($http_x_device_id != "Asset-12345") {
            return 403 "Zero Trust Error: This device is not a registered Company Laptop.";
        }
        
        root /usr/share/nginx/html;
        index index.html;
    }
}
